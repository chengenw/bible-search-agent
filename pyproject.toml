[project]
name = "bible-search-agent"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "chromadb>=0.6",
    "litellm",
    "python-dotenv",
    "google-cloud-aiplatform>=1.152.0",
]
